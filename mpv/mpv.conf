# Loop everything forever (images + GIFs + videos)
loop-file=inf

# Alternative: only static images stay forever, GIFs loop normally
# image-display-duration=inf
# loop-file=inf   # still needed for GIFs to loop forever
